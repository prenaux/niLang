#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct niUIGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct niUIGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct niUIGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct SBO_niUIGpuFuncs_RayInstanceData
{
    niUIGpuFuncs_RayInstanceData_1 v;
};

struct niUIGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct niUIGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    niUIGpuFuncs_VertexRay_1 v[1];
};

struct niUIGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_niUIGpuFuncs_RayUniforms
{
    niUIGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_niUIGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_niUIGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_niUIGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    niUIGpuFuncs_RayUniforms _431;
    _431.rtWidth = IN_1_aUniforms.v.rtWidth;
    _431.rtHeight = IN_1_aUniforms.v.rtHeight;
    _431.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _431.padding = IN_1_aUniforms.v.padding;
    _431.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _431.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _617 = float3(_431.cameraInvView[3].x, _431.cameraInvView[3].y, _431.cameraInvView[3].z);
    float4 _645 = _431.cameraInvViewProj * float4(((gl_FragCoord.x / _431.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _431.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_617, fast::normalize((_645 / float4(_645.w)).xyz - _617), 0.001000000047497451305389404296875, _431.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(0u));
    bool _482 = rayQuery.next();
    uint _483 = uint(rayQuery.get_committed_intersection_type());
    int _484 = rayQuery.get_committed_user_instance_id();
    uint _487 = uint(_484);
    niUIGpuFuncs_RayInstanceData _490;
    _490.ibIndex = nil_builtin_GetRayInstanceData[_487]->v.ibIndex;
    _490.vbIndex = nil_builtin_GetRayInstanceData[_487]->v.vbIndex;
    _490.firstIndex = nil_builtin_GetRayInstanceData[_487]->v.firstIndex;
    _490.baseVertexIndex = nil_builtin_GetRayInstanceData[_487]->v.baseVertexIndex;
    _490.texIndex = nil_builtin_GetRayInstanceData[_487]->v.texIndex;
    _490.materialColor = nil_builtin_GetRayInstanceData[_487]->v.materialColor;
    float4 _855;
    if (_483 == 1u)
    {
        float4 _856;
        if (_490.vbIndex == 0u)
        {
            _856 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _502 = rayQuery.get_committed_primitive_id();
            uint _658 = _490.firstIndex + (3u * uint(_502));
            uint _660 = _490.ibIndex;
            uint _663 = _658;
            uint _670 = _658 + 1u;
            uint _677 = _658 + 2u;
            uint _512 = _490.vbIndex;
            uint _518 = _490.baseVertexIndex + nil_builtin_GetIndex[_660]->v[_663];
            niUIGpuFuncs_VertexRay _521;
            _521.pos = float3(nil_builtin_GetVertexRay[_512]->v[_518].pos);
            _521.normal = float3(nil_builtin_GetVertexRay[_512]->v[_518].normal);
            _521.color = nil_builtin_GetVertexRay[_512]->v[_518].color;
            _521.tex0 = float2(nil_builtin_GetVertexRay[_512]->v[_518].tex0);
            uint _530 = _490.baseVertexIndex + nil_builtin_GetIndex[_660]->v[_670];
            niUIGpuFuncs_VertexRay _533;
            _533.pos = float3(nil_builtin_GetVertexRay[_512]->v[_530].pos);
            _533.normal = float3(nil_builtin_GetVertexRay[_512]->v[_530].normal);
            _533.color = nil_builtin_GetVertexRay[_512]->v[_530].color;
            _533.tex0 = float2(nil_builtin_GetVertexRay[_512]->v[_530].tex0);
            uint _542 = _490.baseVertexIndex + nil_builtin_GetIndex[_660]->v[_677];
            niUIGpuFuncs_VertexRay _545;
            _545.pos = float3(nil_builtin_GetVertexRay[_512]->v[_542].pos);
            _545.normal = float3(nil_builtin_GetVertexRay[_512]->v[_542].normal);
            _545.color = nil_builtin_GetVertexRay[_512]->v[_542].color;
            _545.tex0 = float2(nil_builtin_GetVertexRay[_512]->v[_542].tex0);
            float2 _546 = rayQuery.get_committed_triangle_barycentric_coord();
            _856 = float4(((((_521.normal * ((1.0 - _546.x) - _546.y)) + (_533.normal * _546.x)) + (_545.normal * _546.y)) * 0.5) + float3(0.5), 1.0);
        }
        _855 = _856;
    }
    else
    {
        _855 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_483 == 2u));
    }
    out.OUT_0_rval_color = _855;
    return out;
}

