#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
    uint firstIndex;
    uint baseVertexIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
    uint firstIndex;
    uint baseVertexIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _417;
    _417.rtWidth = IN_1_aUniforms.v.rtWidth;
    _417.rtHeight = IN_1_aUniforms.v.rtHeight;
    _417.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _417.padding = IN_1_aUniforms.v.padding;
    _417.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _417.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _595 = float3(_417.cameraInvView[3].x, _417.cameraInvView[3].y, _417.cameraInvView[3].z);
    float4 _623 = _417.cameraInvViewProj * float4(((gl_FragCoord.x / _417.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _417.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_595, fast::normalize((_623 / float4(_623.w)).xyz - _595), 0.001000000047497451305389404296875, _417.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _468 = rayQuery.next();
    uint _469 = uint(rayQuery.get_committed_intersection_type());
    int _470 = rayQuery.get_committed_user_instance_id();
    uint _473 = uint(_470);
    TestGpuFuncs_RayInstanceData _476;
    _476.ibIndex = nil_builtin_GetRayInstanceData[_473]->v.ibIndex;
    _476.vbIndex = nil_builtin_GetRayInstanceData[_473]->v.vbIndex;
    _476.texIndex = nil_builtin_GetRayInstanceData[_473]->v.texIndex;
    _476.firstIndex = nil_builtin_GetRayInstanceData[_473]->v.firstIndex;
    _476.baseVertexIndex = nil_builtin_GetRayInstanceData[_473]->v.baseVertexIndex;
    float4 _813;
    if (_469 == 1u)
    {
        float4 _814;
        if (_476.vbIndex == 0u)
        {
            _814 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _488 = rayQuery.get_committed_primitive_id();
            uint _636 = _476.firstIndex + (3u * uint(_488));
            uint _638 = _476.ibIndex;
            uint _641 = _636;
            uint _648 = _636 + 1u;
            uint _655 = _636 + 2u;
            uint _498 = _476.vbIndex;
            uint _504 = _476.baseVertexIndex + nil_builtin_GetIndex[_638]->v[_641];
            TestGpuFuncs_VertexRay _507;
            _507.pos = float3(nil_builtin_GetVertexRay[_498]->v[_504].pos);
            _507.normal = float3(nil_builtin_GetVertexRay[_498]->v[_504].normal);
            _507.color = nil_builtin_GetVertexRay[_498]->v[_504].color;
            _507.tex0 = float2(nil_builtin_GetVertexRay[_498]->v[_504].tex0);
            uint _516 = _476.baseVertexIndex + nil_builtin_GetIndex[_638]->v[_648];
            TestGpuFuncs_VertexRay _519;
            _519.pos = float3(nil_builtin_GetVertexRay[_498]->v[_516].pos);
            _519.normal = float3(nil_builtin_GetVertexRay[_498]->v[_516].normal);
            _519.color = nil_builtin_GetVertexRay[_498]->v[_516].color;
            _519.tex0 = float2(nil_builtin_GetVertexRay[_498]->v[_516].tex0);
            uint _528 = _476.baseVertexIndex + nil_builtin_GetIndex[_638]->v[_655];
            TestGpuFuncs_VertexRay _531;
            _531.pos = float3(nil_builtin_GetVertexRay[_498]->v[_528].pos);
            _531.normal = float3(nil_builtin_GetVertexRay[_498]->v[_528].normal);
            _531.color = nil_builtin_GetVertexRay[_498]->v[_528].color;
            _531.tex0 = float2(nil_builtin_GetVertexRay[_498]->v[_528].tex0);
            float2 _532 = rayQuery.get_committed_triangle_barycentric_coord();
            _814 = float4(((((_507.normal * ((1.0 - _532.x) - _532.y)) + (_519.normal * _532.x)) + (_531.normal * _532.y)) * 0.5) + float3(0.5), 1.0);
        }
        _813 = _814;
    }
    else
    {
        _813 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_469 == 2u));
    }
    out.OUT_0_rval_color = _813;
    return out;
}

