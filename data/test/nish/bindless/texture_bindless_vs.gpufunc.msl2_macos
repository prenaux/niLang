#pragma clang diagnostic ignored "-Wmissing-prototypes"

#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

template<typename T>
struct spvDescriptor
{
    T value;
};

template<typename T>
struct spvDescriptorArray
{
    spvDescriptorArray(const device spvDescriptor<T>* ptr) : ptr(&ptr->value)
    {
    }
    const device T& operator [] (size_t i) const
    {
        return ptr[i];
    }
    const device T* ptr;
};

struct TestGpuFuncs_InstanceData
{
    float4x4 mtxWorld;
    uint texIndex0;
    uint texIndex1;
    uint texIndex2;
    uint texIndex3;
};

struct TestGpuFuncs_InstanceData_1
{
    float4x4 mtxWorld;
    uint texIndex0;
    uint texIndex1;
    uint texIndex2;
    uint texIndex3;
};

struct SBO_TestGpuFuncs_InstanceData
{
    TestGpuFuncs_InstanceData_1 v;
};

struct main0_out
{
    float4 OUT_1_rval_color [[user(locn1)]];
    float2 OUT_2_rval_tex0 [[user(locn2)]];
    float4 gl_Position [[position]];
};

struct main0_in
{
    float3 IN_0_aInput_position [[attribute(0)]];
    float4 IN_4_aInput_color [[attribute(4)]];
    float2 IN_5_aInput_tex0 [[attribute(5)]];
};

vertex main0_out main0(main0_in in [[stage_in]], const device spvDescriptor<const device SBO_TestGpuFuncs_InstanceData*>* nil_builtin_GetInstanceData_ [[buffer(0)]], uint gl_InstanceIndex [[instance_id]])
{
    spvDescriptorArray<const device SBO_TestGpuFuncs_InstanceData*> nil_builtin_GetInstanceData {nil_builtin_GetInstanceData_};

    main0_out out = {};
    uint _168 = uint(int(gl_InstanceIndex));
    TestGpuFuncs_InstanceData _171;
    _171.mtxWorld = nil_builtin_GetInstanceData[_168]->v.mtxWorld;
    _171.texIndex0 = nil_builtin_GetInstanceData[_168]->v.texIndex0;
    _171.texIndex1 = nil_builtin_GetInstanceData[_168]->v.texIndex1;
    _171.texIndex2 = nil_builtin_GetInstanceData[_168]->v.texIndex2;
    _171.texIndex3 = nil_builtin_GetInstanceData[_168]->v.texIndex3;
    out.gl_Position = _171.mtxWorld * float4(in.IN_0_aInput_position, 1.0);
    out.OUT_1_rval_color = in.IN_4_aInput_color;
    out.OUT_2_rval_tex0 = in.IN_5_aInput_tex0;
    return out;
}

