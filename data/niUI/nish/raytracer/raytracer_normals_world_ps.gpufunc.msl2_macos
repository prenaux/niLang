#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct niUIGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct niUIGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct niUIGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct SBO_niUIGpuFuncs_RayInstanceData
{
    niUIGpuFuncs_RayInstanceData_1 v;
};

struct niUIGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct niUIGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    niUIGpuFuncs_VertexRay_1 v[1];
};

struct niUIGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_niUIGpuFuncs_RayUniforms
{
    niUIGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_niUIGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_niUIGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_niUIGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    niUIGpuFuncs_RayUniforms _460;
    _460.rtWidth = IN_1_aUniforms.v.rtWidth;
    _460.rtHeight = IN_1_aUniforms.v.rtHeight;
    _460.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _460.padding = IN_1_aUniforms.v.padding;
    _460.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _460.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _653 = float3(_460.cameraInvView[3].x, _460.cameraInvView[3].y, _460.cameraInvView[3].z);
    float4 _681 = _460.cameraInvViewProj * float4(((gl_FragCoord.x / _460.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _460.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_653, fast::normalize((_681 / float4(_681.w)).xyz - _653), 0.001000000047497451305389404296875, _460.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(0u));
    bool _514 = rayQuery.next();
    uint _515 = uint(rayQuery.get_committed_intersection_type());
    int _516 = rayQuery.get_committed_user_instance_id();
    uint _519 = uint(_516);
    niUIGpuFuncs_RayInstanceData _522;
    _522.ibIndex = nil_builtin_GetRayInstanceData[_519]->v.ibIndex;
    _522.vbIndex = nil_builtin_GetRayInstanceData[_519]->v.vbIndex;
    _522.firstIndex = nil_builtin_GetRayInstanceData[_519]->v.firstIndex;
    _522.baseVertexIndex = nil_builtin_GetRayInstanceData[_519]->v.baseVertexIndex;
    _522.texIndex = nil_builtin_GetRayInstanceData[_519]->v.texIndex;
    _522.materialColor = nil_builtin_GetRayInstanceData[_519]->v.materialColor;
    float4 _909;
    if (_515 == 1u)
    {
        float4 _910;
        if (_522.vbIndex == 0u)
        {
            _910 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _534 = rayQuery.get_committed_primitive_id();
            uint _694 = _522.firstIndex + (3u * uint(_534));
            uint _696 = _522.ibIndex;
            uint _699 = _694;
            uint _706 = _694 + 1u;
            uint _713 = _694 + 2u;
            uint _544 = _522.vbIndex;
            uint _550 = _522.baseVertexIndex + nil_builtin_GetIndex[_696]->v[_699];
            niUIGpuFuncs_VertexRay _553;
            _553.pos = float3(nil_builtin_GetVertexRay[_544]->v[_550].pos);
            _553.normal = float3(nil_builtin_GetVertexRay[_544]->v[_550].normal);
            _553.color = nil_builtin_GetVertexRay[_544]->v[_550].color;
            _553.tex0 = float2(nil_builtin_GetVertexRay[_544]->v[_550].tex0);
            uint _562 = _522.baseVertexIndex + nil_builtin_GetIndex[_696]->v[_706];
            niUIGpuFuncs_VertexRay _565;
            _565.pos = float3(nil_builtin_GetVertexRay[_544]->v[_562].pos);
            _565.normal = float3(nil_builtin_GetVertexRay[_544]->v[_562].normal);
            _565.color = nil_builtin_GetVertexRay[_544]->v[_562].color;
            _565.tex0 = float2(nil_builtin_GetVertexRay[_544]->v[_562].tex0);
            uint _574 = _522.baseVertexIndex + nil_builtin_GetIndex[_696]->v[_713];
            niUIGpuFuncs_VertexRay _577;
            _577.pos = float3(nil_builtin_GetVertexRay[_544]->v[_574].pos);
            _577.normal = float3(nil_builtin_GetVertexRay[_544]->v[_574].normal);
            _577.color = nil_builtin_GetVertexRay[_544]->v[_574].color;
            _577.tex0 = float2(nil_builtin_GetVertexRay[_544]->v[_574].tex0);
            float2 _578 = rayQuery.get_committed_triangle_barycentric_coord();
            float4x3 _589 = rayQuery.get_committed_object_to_world_transform();
            _910 = float4(((_589 * float4(fast::normalize(((_553.normal * ((1.0 - _578.x) - _578.y)) + (_565.normal * _578.x)) + (_577.normal * _578.y)), 0.0)) * 0.5) + float3(0.5), 1.0);
        }
        _909 = _910;
    }
    else
    {
        _909 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_515 == 2u));
    }
    out.OUT_0_rval_color = _909;
    return out;
}

