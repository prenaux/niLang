#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _402;
    _402.rtWidth = IN_1_aUniforms.v.rtWidth;
    _402.rtHeight = IN_1_aUniforms.v.rtHeight;
    _402.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _402.padding = IN_1_aUniforms.v.padding;
    _402.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _402.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _568 = float3(_402.cameraInvView[3].x, _402.cameraInvView[3].y, _402.cameraInvView[3].z);
    float4 _596 = _402.cameraInvViewProj * float4(((gl_FragCoord.x / _402.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _402.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_568, fast::normalize((_596 / float4(_596.w)).xyz - _568), 0.001000000047497451305389404296875, _402.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _452 = rayQuery.next();
    uint _453 = uint(rayQuery.get_committed_intersection_type());
    int _454 = rayQuery.get_committed_user_instance_id();
    uint _457 = uint(_454);
    TestGpuFuncs_RayInstanceData _460;
    _460.ibIndex = nil_builtin_GetRayInstanceData[_457]->v.ibIndex;
    _460.vbIndex = nil_builtin_GetRayInstanceData[_457]->v.vbIndex;
    _460.texIndex = nil_builtin_GetRayInstanceData[_457]->v.texIndex;
    float4 _780;
    if (_453 == 1u)
    {
        float4 _781;
        if (_460.vbIndex == 0u)
        {
            _781 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _472 = rayQuery.get_committed_primitive_id();
            uint _607 = 3u * uint(_472);
            uint _609 = _460.ibIndex;
            uint _612 = _607;
            uint _619 = _607 + 1u;
            uint _626 = _607 + 2u;
            uint _480 = _460.vbIndex;
            TestGpuFuncs_VertexRay _486;
            _486.pos = float3(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_612]].pos);
            _486.normal = float3(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_612]].normal);
            _486.color = nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_612]].color;
            _486.tex0 = float2(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_612]].tex0);
            TestGpuFuncs_VertexRay _495;
            _495.pos = float3(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_619]].pos);
            _495.normal = float3(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_619]].normal);
            _495.color = nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_619]].color;
            _495.tex0 = float2(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_619]].tex0);
            TestGpuFuncs_VertexRay _504;
            _504.pos = float3(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_626]].pos);
            _504.normal = float3(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_626]].normal);
            _504.color = nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_626]].color;
            _504.tex0 = float2(nil_builtin_GetVertexRay[_480]->v[nil_builtin_GetIndex[_609]->v[_626]].tex0);
            float2 _505 = rayQuery.get_committed_triangle_barycentric_coord();
            _781 = float4(((((_486.normal * ((1.0 - _505.x) - _505.y)) + (_495.normal * _505.x)) + (_504.normal * _505.y)) * 0.5) + float3(0.5), 1.0);
        }
        _780 = _781;
    }
    else
    {
        _780 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_453 == 2u));
    }
    out.OUT_0_rval_color = _780;
    return out;
}

