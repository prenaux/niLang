#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

// Implementation of the GLSL mod() function, which is slightly different than Metal fmod()
template<typename Tx, typename Ty>
inline Tx mod(Tx x, Ty y)
{
    return x - y * floor(x / y);
}

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
    uint firstIndex;
    uint baseVertexIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
    uint firstIndex;
    uint baseVertexIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _635;
    _635.rtWidth = IN_1_aUniforms.v.rtWidth;
    _635.rtHeight = IN_1_aUniforms.v.rtHeight;
    _635.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _635.padding = IN_1_aUniforms.v.padding;
    _635.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _635.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _850 = float3(_635.cameraInvView[3].x, _635.cameraInvView[3].y, _635.cameraInvView[3].z);
    float4 _878 = _635.cameraInvViewProj * float4(((gl_FragCoord.x / _635.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _635.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_850, fast::normalize((_878 / float4(_878.w)).xyz - _850), 0.001000000047497451305389404296875, _635.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _703 = rayQuery.next();
    uint _704 = uint(rayQuery.get_committed_intersection_type());
    int _705 = rayQuery.get_committed_user_instance_id();
    uint _708 = uint(_705);
    TestGpuFuncs_RayInstanceData _711;
    _711.ibIndex = nil_builtin_GetRayInstanceData[_708]->v.ibIndex;
    _711.vbIndex = nil_builtin_GetRayInstanceData[_708]->v.vbIndex;
    _711.texIndex = nil_builtin_GetRayInstanceData[_708]->v.texIndex;
    _711.firstIndex = nil_builtin_GetRayInstanceData[_708]->v.firstIndex;
    _711.baseVertexIndex = nil_builtin_GetRayInstanceData[_708]->v.baseVertexIndex;
    float4 _1271;
    if (_704 == 1u)
    {
        float4 _1272;
        if (_711.vbIndex == 0u)
        {
            _1272 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _723 = rayQuery.get_committed_primitive_id();
            uint _891 = _711.firstIndex + (3u * uint(_723));
            uint _893 = _711.ibIndex;
            uint _896 = _891;
            uint _903 = _891 + 1u;
            uint _910 = _891 + 2u;
            uint _733 = _711.vbIndex;
            uint _739 = _711.baseVertexIndex + nil_builtin_GetIndex[_893]->v[_896];
            TestGpuFuncs_VertexRay _742;
            _742.pos = float3(nil_builtin_GetVertexRay[_733]->v[_739].pos);
            _742.normal = float3(nil_builtin_GetVertexRay[_733]->v[_739].normal);
            _742.color = nil_builtin_GetVertexRay[_733]->v[_739].color;
            _742.tex0 = float2(nil_builtin_GetVertexRay[_733]->v[_739].tex0);
            uint _751 = _711.baseVertexIndex + nil_builtin_GetIndex[_893]->v[_903];
            TestGpuFuncs_VertexRay _754;
            _754.pos = float3(nil_builtin_GetVertexRay[_733]->v[_751].pos);
            _754.normal = float3(nil_builtin_GetVertexRay[_733]->v[_751].normal);
            _754.color = nil_builtin_GetVertexRay[_733]->v[_751].color;
            _754.tex0 = float2(nil_builtin_GetVertexRay[_733]->v[_751].tex0);
            uint _763 = _711.baseVertexIndex + nil_builtin_GetIndex[_893]->v[_910];
            TestGpuFuncs_VertexRay _766;
            _766.pos = float3(nil_builtin_GetVertexRay[_733]->v[_763].pos);
            _766.normal = float3(nil_builtin_GetVertexRay[_733]->v[_763].normal);
            _766.color = nil_builtin_GetVertexRay[_733]->v[_763].color;
            _766.tex0 = float2(nil_builtin_GetVertexRay[_733]->v[_763].tex0);
            float2 _767 = rayQuery.get_committed_triangle_barycentric_coord();
            float _921 = (1.0 - _767.x) - _767.y;
            float4x3 _778 = rayQuery.get_committed_object_to_world_transform();
            float4x3 _789 = rayQuery.get_committed_object_to_world_transform();
            float3 _986 = _789 * float4(((_742.pos * _921) + (_754.pos * _767.x)) + (_766.pos * _767.y), 1.0);
            float3 _998 = floor(_986 * float3(0.100000001490116119384765625));
            float3 _1000 = abs(_778 * float4(fast::normalize(((_742.normal * _921) + (_754.normal * _767.x)) + (_766.normal * _767.y)), 0.0));
            float _1002 = _1000.x;
            float _1004 = _1000.y;
            bool _1005 = _1002 > _1004;
            bool _1013;
            if (_1005)
            {
                _1013 = _1002 > _1000.z;
            }
            else
            {
                _1013 = _1005;
            }
            float _1267;
            if (_1013)
            {
                _1267 = mod(_998.y + _998.z, 2.0);
            }
            else
            {
                float _1268;
                if (_1004 > _1000.z)
                {
                    _1268 = mod(_998.x + _998.z, 2.0);
                }
                else
                {
                    _1268 = mod(_998.x + _998.y, 2.0);
                }
                _1267 = _1268;
            }
            float _1064 = length(_986);
            float3 _1269;
            if (_1064 < 100.0)
            {
                _1269 = mix(float3(0.0, 1.0, 0.0), float3(1.0, 0.0, 0.0), float3(_1064 * 0.00999999977648258209228515625));
            }
            else
            {
                float _1087 = _1064 - 100.0;
                float _1093 = mod(_1087, 200.0) * 0.004999999888241291046142578125;
                float3 _1270;
                if (mod(floor(_1087 * 0.004999999888241291046142578125), 2.0) < 0.5)
                {
                    _1270 = mix(float3(1.0, 0.0, 0.0), float3(0.0, 0.0, 1.0), float3(_1093));
                }
                else
                {
                    _1270 = mix(float3(0.0, 0.0, 1.0), float3(1.0, 0.0, 0.0), float3(_1093));
                }
                _1269 = _1270;
            }
            _1272 = float4(_1269 * mix(1.0, 0.20000000298023223876953125, step(0.5, _1267)), 1.0);
        }
        _1271 = _1272;
    }
    else
    {
        _1271 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_704 == 2u));
    }
    out.OUT_0_rval_color = _1271;
    return out;
}

