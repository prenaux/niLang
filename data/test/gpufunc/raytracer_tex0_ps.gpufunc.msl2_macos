#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _420;
    _420.rtWidth = IN_1_aUniforms.v.rtWidth;
    _420.rtHeight = IN_1_aUniforms.v.rtHeight;
    _420.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _420.padding = IN_1_aUniforms.v.padding;
    _420.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _420.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _614 = float3(_420.cameraInvView[3].x, _420.cameraInvView[3].y, _420.cameraInvView[3].z);
    float4 _642 = _420.cameraInvViewProj * float4(((gl_FragCoord.x / _420.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _420.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_614, fast::normalize((_642 / float4(_642.w)).xyz - _614), 0.001000000047497451305389404296875, _420.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(0u));
    bool _473 = rayQuery.next();
    uint _474 = uint(rayQuery.get_committed_intersection_type());
    int _475 = rayQuery.get_committed_user_instance_id();
    uint _478 = uint(_475);
    TestGpuFuncs_RayInstanceData _481;
    _481.ibIndex = nil_builtin_GetRayInstanceData[_478]->v.ibIndex;
    _481.vbIndex = nil_builtin_GetRayInstanceData[_478]->v.vbIndex;
    float4 _822;
    if (_474 == 1u)
    {
        float4 _823;
        if (_481.vbIndex == 0u)
        {
            _823 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _493 = rayQuery.get_committed_primitive_id();
            uint _653 = 3u * uint(_493);
            uint _655 = _481.ibIndex;
            uint _658 = _653;
            uint _665 = _653 + 1u;
            uint _672 = _653 + 2u;
            uint _501 = _481.vbIndex;
            TestGpuFuncs_VertexRay _507;
            _507.pos = float3(nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_658]].pos);
            _507.normal = float3(nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_658]].normal);
            _507.tex0 = nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_658]].tex0;
            TestGpuFuncs_VertexRay _516;
            _516.pos = float3(nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_665]].pos);
            _516.normal = float3(nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_665]].normal);
            _516.tex0 = nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_665]].tex0;
            TestGpuFuncs_VertexRay _525;
            _525.pos = float3(nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_672]].pos);
            _525.normal = float3(nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_672]].normal);
            _525.tex0 = nil_builtin_GetVertexRay[_501]->v[nil_builtin_GetIndex[_655]->v[_672]].tex0;
            float2 _526 = rayQuery.get_committed_triangle_barycentric_coord();
            float _683 = (1.0 - _526.x) - _526.y;
            float2 _819;
            if (_683 > 0.85000002384185791015625)
            {
                _819 = _507.tex0;
            }
            else
            {
                float2 _820;
                if (_526.x > 0.85000002384185791015625)
                {
                    _820 = _516.tex0;
                }
                else
                {
                    _820 = select(((_507.tex0 * _683) + (_516.tex0 * _526.x)) + (_525.tex0 * _526.y), _525.tex0, bool2(_526.y > 0.85000002384185791015625));
                }
                _819 = _820;
            }
            _823 = float4(_819, 0.0, 1.0);
        }
        _822 = _823;
    }
    else
    {
        _822 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_474 == 2u));
    }
    out.OUT_0_rval_color = _822;
    return out;
}

