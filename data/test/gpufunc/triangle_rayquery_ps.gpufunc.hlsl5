struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    row_major float4x4 cameraInvView;
    row_major float4x4 cameraInvViewProj;
};

cbuffer UBO_TestGpuFuncs_RayUniforms : register(b0)
{
    TestGpuFuncs_RayUniforms IN_1_aUniforms_v : packoffset(c0);
};

uniform RaytracingAccelerationStructure IN_1_aAS : register(t0);

static float4 gl_FragCoord;
static float4 OUT_0_rval_color;

struct SPIRV_Cross_Input
{
    float4 gl_FragCoord : SV_Position;
};

struct SPIRV_Cross_Output
{
    float4 OUT_0_rval_color : SV_Target0;
};

static RayQuery<RAY_FLAG_NONE> rayQuery;

void frag_main()
{
    TestGpuFuncs_RayUniforms _269;
    _269.rtWidth = IN_1_aUniforms_v.rtWidth;
    _269.rtHeight = IN_1_aUniforms_v.rtHeight;
    _269.cameraFarClipPlane = IN_1_aUniforms_v.cameraFarClipPlane;
    _269.padding = IN_1_aUniforms_v.padding;
    _269.cameraInvView = IN_1_aUniforms_v.cameraInvView;
    _269.cameraInvViewProj = IN_1_aUniforms_v.cameraInvViewProj;
    float3 _327 = float3(_269.cameraInvView[3].x, _269.cameraInvView[3].y, _269.cameraInvView[3].z);
    float4 _385 = mul(float4(((gl_FragCoord.x / _269.rtWidth) * 2.0f) - 1.0f, 1.0f - ((gl_FragCoord.y / _269.rtHeight) * 2.0f), 1.0f, 1.0f), _269.cameraInvViewProj);
    RayDesc _1ident = {_327, 0.001000000047497451305389404296875f, normalize((_385 / _385.w.xxxx).xyz - _327), _269.cameraFarClipPlane};
    rayQuery.TraceRayInline(IN_1_aAS, 0u, 255u, _1ident);
    bool _342 = rayQuery.Proceed();
    uint _343 = rayQuery.CommittedStatus();
    int _344 = rayQuery.CommittedInstanceIndex();
    uint _346 = uint(_344) + 1u;
    uint _421 = _346 + (_346 << uint(10));
    uint _425 = _421 ^ (_421 >> uint(6));
    uint _429 = _425 + (_425 << uint(3));
    uint _433 = _429 ^ (_429 >> uint(11));
    uint _437 = _433 + (_433 << uint(15));
    float4 _511;
    if (_343 == 1u)
    {
        _511 = float4(float((_437 >> uint(0)) & 255u) * 0.0039215688593685626983642578125f, float((_437 >> uint(8)) & 255u) * 0.0039215688593685626983642578125f, float((_437 >> uint(16)) & 255u) * 0.0039215688593685626983642578125f, 1.0f);
    }
    else
    {
        bool4 _514 = (_343 == 2u).xxxx;
        _511 = float4(_514.x ? float4(1.0f, 0.0f, 0.0f, 1.0f).x : float4(0.0f, 0.5f, 0.800000011920928955078125f, 0.0f).x, _514.y ? float4(1.0f, 0.0f, 0.0f, 1.0f).y : float4(0.0f, 0.5f, 0.800000011920928955078125f, 0.0f).y, _514.z ? float4(1.0f, 0.0f, 0.0f, 1.0f).z : float4(0.0f, 0.5f, 0.800000011920928955078125f, 0.0f).z, _514.w ? float4(1.0f, 0.0f, 0.0f, 1.0f).w : float4(0.0f, 0.5f, 0.800000011920928955078125f, 0.0f).w);
    }
    OUT_0_rval_color = _511;
}

SPIRV_Cross_Output main(SPIRV_Cross_Input stage_input)
{
    gl_FragCoord = stage_input.gl_FragCoord;
    gl_FragCoord.w = 1.0 / gl_FragCoord.w;
    frag_main();
    SPIRV_Cross_Output stage_output;
    stage_output.OUT_0_rval_color = OUT_0_rval_color;
    return stage_output;
}
