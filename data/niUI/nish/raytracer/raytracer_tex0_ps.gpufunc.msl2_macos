#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _421;
    _421.rtWidth = IN_1_aUniforms.v.rtWidth;
    _421.rtHeight = IN_1_aUniforms.v.rtHeight;
    _421.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _421.padding = IN_1_aUniforms.v.padding;
    _421.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _421.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _615 = float3(_421.cameraInvView[3].x, _421.cameraInvView[3].y, _421.cameraInvView[3].z);
    float4 _643 = _421.cameraInvViewProj * float4(((gl_FragCoord.x / _421.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _421.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_615, fast::normalize((_643 / float4(_643.w)).xyz - _615), 0.001000000047497451305389404296875, _421.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _474 = rayQuery.next();
    uint _475 = uint(rayQuery.get_committed_intersection_type());
    int _476 = rayQuery.get_committed_user_instance_id();
    uint _479 = uint(_476);
    TestGpuFuncs_RayInstanceData _482;
    _482.ibIndex = nil_builtin_GetRayInstanceData[_479]->v.ibIndex;
    _482.vbIndex = nil_builtin_GetRayInstanceData[_479]->v.vbIndex;
    _482.texIndex = nil_builtin_GetRayInstanceData[_479]->v.texIndex;
    float4 _824;
    if (_475 == 1u)
    {
        float4 _825;
        if (_482.vbIndex == 0u)
        {
            _825 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _494 = rayQuery.get_committed_primitive_id();
            uint _654 = 3u * uint(_494);
            uint _656 = _482.ibIndex;
            uint _659 = _654;
            uint _666 = _654 + 1u;
            uint _673 = _654 + 2u;
            uint _502 = _482.vbIndex;
            TestGpuFuncs_VertexRay _508;
            _508.pos = float3(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_659]].pos);
            _508.normal = float3(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_659]].normal);
            _508.color = nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_659]].color;
            _508.tex0 = float2(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_659]].tex0);
            TestGpuFuncs_VertexRay _517;
            _517.pos = float3(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_666]].pos);
            _517.normal = float3(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_666]].normal);
            _517.color = nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_666]].color;
            _517.tex0 = float2(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_666]].tex0);
            TestGpuFuncs_VertexRay _526;
            _526.pos = float3(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_673]].pos);
            _526.normal = float3(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_673]].normal);
            _526.color = nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_673]].color;
            _526.tex0 = float2(nil_builtin_GetVertexRay[_502]->v[nil_builtin_GetIndex[_656]->v[_673]].tex0);
            float2 _527 = rayQuery.get_committed_triangle_barycentric_coord();
            float _684 = (1.0 - _527.x) - _527.y;
            float2 _821;
            if (_684 > 0.85000002384185791015625)
            {
                _821 = _508.tex0;
            }
            else
            {
                float2 _822;
                if (_527.x > 0.85000002384185791015625)
                {
                    _822 = _517.tex0;
                }
                else
                {
                    _822 = select(((_508.tex0 * _684) + (_517.tex0 * _527.x)) + (_526.tex0 * _527.y), _526.tex0, bool2(_527.y > 0.85000002384185791015625));
                }
                _821 = _822;
            }
            _825 = float4(_821, 0.0, 1.0);
        }
        _824 = _825;
    }
    else
    {
        _824 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_475 == 2u));
    }
    out.OUT_0_rval_color = _824;
    return out;
}

