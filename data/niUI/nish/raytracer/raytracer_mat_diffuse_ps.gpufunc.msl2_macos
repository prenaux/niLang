#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

template<typename T>
struct spvDescriptor
{
    T value;
};

template<typename T>
struct spvDescriptorArray
{
    spvDescriptorArray(const device spvDescriptor<T>* ptr) : ptr(&ptr->value)
    {
    }
    const device T& operator [] (size_t i) const
    {
        return ptr[i];
    }
    const device T* ptr;
};

struct niUIGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct niUIGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct niUIGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct SBO_niUIGpuFuncs_RayInstanceData
{
    niUIGpuFuncs_RayInstanceData_1 v;
};

struct niUIGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct niUIGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    niUIGpuFuncs_VertexRay_1 v[1];
};

struct SBO_GetIndex
{
    uint v[1];
};

struct niUIGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_niUIGpuFuncs_RayUniforms
{
    niUIGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(1)]], constant UBO_niUIGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(2)]], const device spvDescriptor<texture2d<float>>* nil_builtin_GetTexture2D_ [[buffer(0)]], sampler IN_1_aSS [[sampler(0)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(3)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<texture2d<float>> nil_builtin_GetTexture2D {nil_builtin_GetTexture2D_};
    spvDescriptorArray<const device SBO_niUIGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};

    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_niUIGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    niUIGpuFuncs_RayUniforms _853;
    _853.rtWidth = IN_1_aUniforms.v.rtWidth;
    _853.rtHeight = IN_1_aUniforms.v.rtHeight;
    _853.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _853.padding = IN_1_aUniforms.v.padding;
    _853.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _853.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _985 = float3(_853.cameraInvView[3].x, _853.cameraInvView[3].y, _853.cameraInvView[3].z);
    float4 _1103 = _853.cameraInvViewProj * float4(((gl_FragCoord.x / _853.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _853.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_985, fast::normalize((_1103 / float4(_1103.w)).xyz - _985), 0.001000000047497451305389404296875, _853.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(0u));
    bool _1120 = rayQuery.next();
    uint _1167 = uint(rayQuery.get_committed_intersection_type());
    int _1168 = rayQuery.get_committed_user_instance_id();
    uint _1173 = uint(_1168);
    niUIGpuFuncs_RayInstanceData _1177;
    _1177.ibIndex = nil_builtin_GetRayInstanceData[_1173]->v.ibIndex;
    _1177.vbIndex = nil_builtin_GetRayInstanceData[_1173]->v.vbIndex;
    _1177.firstIndex = nil_builtin_GetRayInstanceData[_1173]->v.firstIndex;
    _1177.baseVertexIndex = nil_builtin_GetRayInstanceData[_1173]->v.baseVertexIndex;
    _1177.texIndex = nil_builtin_GetRayInstanceData[_1173]->v.texIndex;
    _1177.materialColor = nil_builtin_GetRayInstanceData[_1173]->v.materialColor;
    float2 _2125;
    int _2131;
    if (_1167 == 1u)
    {
        int _1183 = rayQuery.get_committed_primitive_id();
        float2 _1186 = rayQuery.get_committed_triangle_barycentric_coord();
        float4x3 _1187 = rayQuery.get_committed_object_to_world_transform();
        bool _1240 = _1177.vbIndex == 0u;
        float2 _2123;
        if (_1240)
        {
            _2123 = float2(0.0);
        }
        else
        {
            uint _1330 = _1177.firstIndex + (3u * uint(_1183));
            uint _1332 = _1177.ibIndex;
            uint _1335 = _1330;
            uint _1342 = _1330 + 1u;
            uint _1349 = _1330 + 2u;
            uint _1253 = _1177.vbIndex;
            uint _1258 = _1177.baseVertexIndex + nil_builtin_GetIndex[_1332]->v[_1335];
            niUIGpuFuncs_VertexRay _1261;
            _1261.pos = float3(nil_builtin_GetVertexRay[_1253]->v[_1258].pos);
            _1261.normal = float3(nil_builtin_GetVertexRay[_1253]->v[_1258].normal);
            _1261.color = nil_builtin_GetVertexRay[_1253]->v[_1258].color;
            _1261.tex0 = float2(nil_builtin_GetVertexRay[_1253]->v[_1258].tex0);
            uint _1268 = _1177.baseVertexIndex + nil_builtin_GetIndex[_1332]->v[_1342];
            niUIGpuFuncs_VertexRay _1271;
            _1271.pos = float3(nil_builtin_GetVertexRay[_1253]->v[_1268].pos);
            _1271.normal = float3(nil_builtin_GetVertexRay[_1253]->v[_1268].normal);
            _1271.color = nil_builtin_GetVertexRay[_1253]->v[_1268].color;
            _1271.tex0 = float2(nil_builtin_GetVertexRay[_1253]->v[_1268].tex0);
            uint _1278 = _1177.baseVertexIndex + nil_builtin_GetIndex[_1332]->v[_1349];
            niUIGpuFuncs_VertexRay _1281;
            _1281.pos = float3(nil_builtin_GetVertexRay[_1253]->v[_1278].pos);
            _1281.normal = float3(nil_builtin_GetVertexRay[_1253]->v[_1278].normal);
            _1281.color = nil_builtin_GetVertexRay[_1253]->v[_1278].color;
            _1281.tex0 = float2(nil_builtin_GetVertexRay[_1253]->v[_1278].tex0);
            _2123 = ((_1261.tex0 * ((1.0 - _1186.x) - _1186.y)) + (_1271.tex0 * _1186.x)) + (_1281.tex0 * _1186.y);
        }
        _2131 = _1240 ? (-1) : 1;
        _2125 = _2123;
    }
    else
    {
        _2131 = (_1167 == 2u) ? 2 : 0;
        _2125 = float2(0.0);
    }
    float4 _2134;
    if (_2131 == 1)
    {
        float4 _1457 = unpack_unorm4x8_to_float(_1177.materialColor).zyxw;
        float4 _2133;
        if (_1177.texIndex > 0u)
        {
            uint _1465 = _1177.texIndex;
            _2133 = nil_builtin_GetTexture2D[_1465].sample(IN_1_aSS, _2125) * _1457;
        }
        else
        {
            _2133 = _1457;
        }
        _2134 = _2133;
    }
    else
    {
        float4 _2135;
        if (_2131 == 2)
        {
            _2135 = float4(0.5, 0.0, 0.5, 1.0);
        }
        else
        {
            _2135 = select(float4(0.0), float4(1.0, 0.0, 1.0, 1.0), bool4(_2131 == (-1)));
        }
        _2134 = _2135;
    }
    out.OUT_0_rval_color = _2134;
    return out;
}

