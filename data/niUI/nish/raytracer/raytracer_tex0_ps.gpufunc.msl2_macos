#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct niUIGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct niUIGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct niUIGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct SBO_niUIGpuFuncs_RayInstanceData
{
    niUIGpuFuncs_RayInstanceData_1 v;
};

struct niUIGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct niUIGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    niUIGpuFuncs_VertexRay_1 v[1];
};

struct niUIGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_niUIGpuFuncs_RayUniforms
{
    niUIGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_niUIGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_niUIGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_niUIGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    niUIGpuFuncs_RayUniforms _436;
    _436.rtWidth = IN_1_aUniforms.v.rtWidth;
    _436.rtHeight = IN_1_aUniforms.v.rtHeight;
    _436.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _436.padding = IN_1_aUniforms.v.padding;
    _436.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _436.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _642 = float3(_436.cameraInvView[3].x, _436.cameraInvView[3].y, _436.cameraInvView[3].z);
    float4 _670 = _436.cameraInvViewProj * float4(((gl_FragCoord.x / _436.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _436.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_642, fast::normalize((_670 / float4(_670.w)).xyz - _642), 0.001000000047497451305389404296875, _436.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _490 = rayQuery.next();
    uint _491 = uint(rayQuery.get_committed_intersection_type());
    int _492 = rayQuery.get_committed_user_instance_id();
    uint _495 = uint(_492);
    niUIGpuFuncs_RayInstanceData _498;
    _498.ibIndex = nil_builtin_GetRayInstanceData[_495]->v.ibIndex;
    _498.vbIndex = nil_builtin_GetRayInstanceData[_495]->v.vbIndex;
    _498.firstIndex = nil_builtin_GetRayInstanceData[_495]->v.firstIndex;
    _498.baseVertexIndex = nil_builtin_GetRayInstanceData[_495]->v.baseVertexIndex;
    _498.texIndex = nil_builtin_GetRayInstanceData[_495]->v.texIndex;
    _498.materialColor = nil_builtin_GetRayInstanceData[_495]->v.materialColor;
    float4 _857;
    if (_491 == 1u)
    {
        float4 _858;
        if (_498.vbIndex == 0u)
        {
            _858 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _510 = rayQuery.get_committed_primitive_id();
            uint _683 = _498.firstIndex + (3u * uint(_510));
            uint _685 = _498.ibIndex;
            uint _688 = _683;
            uint _695 = _683 + 1u;
            uint _702 = _683 + 2u;
            uint _520 = _498.vbIndex;
            uint _526 = _498.baseVertexIndex + nil_builtin_GetIndex[_685]->v[_688];
            niUIGpuFuncs_VertexRay _529;
            _529.pos = float3(nil_builtin_GetVertexRay[_520]->v[_526].pos);
            _529.normal = float3(nil_builtin_GetVertexRay[_520]->v[_526].normal);
            _529.color = nil_builtin_GetVertexRay[_520]->v[_526].color;
            _529.tex0 = float2(nil_builtin_GetVertexRay[_520]->v[_526].tex0);
            uint _538 = _498.baseVertexIndex + nil_builtin_GetIndex[_685]->v[_695];
            niUIGpuFuncs_VertexRay _541;
            _541.pos = float3(nil_builtin_GetVertexRay[_520]->v[_538].pos);
            _541.normal = float3(nil_builtin_GetVertexRay[_520]->v[_538].normal);
            _541.color = nil_builtin_GetVertexRay[_520]->v[_538].color;
            _541.tex0 = float2(nil_builtin_GetVertexRay[_520]->v[_538].tex0);
            uint _550 = _498.baseVertexIndex + nil_builtin_GetIndex[_685]->v[_702];
            niUIGpuFuncs_VertexRay _553;
            _553.pos = float3(nil_builtin_GetVertexRay[_520]->v[_550].pos);
            _553.normal = float3(nil_builtin_GetVertexRay[_520]->v[_550].normal);
            _553.color = nil_builtin_GetVertexRay[_520]->v[_550].color;
            _553.tex0 = float2(nil_builtin_GetVertexRay[_520]->v[_550].tex0);
            float2 _554 = rayQuery.get_committed_triangle_barycentric_coord();
            float _713 = (1.0 - _554.x) - _554.y;
            float2 _854;
            if (_713 > 0.85000002384185791015625)
            {
                _854 = _529.tex0;
            }
            else
            {
                float2 _855;
                if (_554.x > 0.85000002384185791015625)
                {
                    _855 = _541.tex0;
                }
                else
                {
                    _855 = select(((_529.tex0 * _713) + (_541.tex0 * _554.x)) + (_553.tex0 * _554.y), _553.tex0, bool2(_554.y > 0.85000002384185791015625));
                }
                _854 = _855;
            }
            _858 = float4(_854, 0.0, 1.0);
        }
        _857 = _858;
    }
    else
    {
        _857 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_491 == 2u));
    }
    out.OUT_0_rval_color = _857;
    return out;
}

