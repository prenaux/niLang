#pragma clang diagnostic ignored "-Wmissing-prototypes"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(0)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(1)]], float4 gl_FragCoord [[position]])
{
    main0_out out = {};
    TestGpuFuncs_RayUniforms _281;
    _281.rtWidth = IN_1_aUniforms.v.rtWidth;
    _281.rtHeight = IN_1_aUniforms.v.rtHeight;
    _281.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _281.padding = IN_1_aUniforms.v.padding;
    _281.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _281.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _374 = float3(_281.cameraInvView[3].x, _281.cameraInvView[3].y, _281.cameraInvView[3].z);
    float4 _402 = _281.cameraInvViewProj * float4(((gl_FragCoord.x / _281.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _281.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_374, fast::normalize((_402 / float4(_402.w)).xyz - _374), 0.001000000047497451305389404296875, _281.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _314 = rayQuery.next();
    uint _315 = uint(rayQuery.get_committed_intersection_type());
    int _316 = rayQuery.get_committed_user_instance_id();
    uint _317 = uint(_316);
    uint _438 = _317 + (_317 << uint(10));
    uint _442 = _438 ^ (_438 >> uint(6));
    uint _446 = _442 + (_442 << uint(3));
    uint _450 = _446 ^ (_446 >> uint(11));
    uint _454 = _450 + (_450 << uint(15));
    float4 _548;
    if (_315 == 1u)
    {
        _548 = float4(float((_454 >> uint(0)) & 255u) * 0.0039215688593685626983642578125, float((_454 >> uint(8)) & 255u) * 0.0039215688593685626983642578125, float((_454 >> uint(16)) & 255u) * 0.0039215688593685626983642578125, 1.0);
    }
    else
    {
        _548 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_315 == 2u));
    }
    out.OUT_0_rval_color = _548;
    return out;
}

