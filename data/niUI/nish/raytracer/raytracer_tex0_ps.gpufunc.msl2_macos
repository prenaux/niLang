#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct niUIGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct niUIGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct niUIGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct SBO_niUIGpuFuncs_RayInstanceData
{
    niUIGpuFuncs_RayInstanceData_1 v;
};

struct niUIGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct niUIGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    niUIGpuFuncs_VertexRay_1 v[1];
};

struct niUIGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_niUIGpuFuncs_RayUniforms
{
    niUIGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_niUIGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_niUIGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_niUIGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    niUIGpuFuncs_RayUniforms _450;
    _450.rtWidth = IN_1_aUniforms.v.rtWidth;
    _450.rtHeight = IN_1_aUniforms.v.rtHeight;
    _450.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _450.padding = IN_1_aUniforms.v.padding;
    _450.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _450.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _664 = float3(_450.cameraInvView[3].x, _450.cameraInvView[3].y, _450.cameraInvView[3].z);
    float4 _692 = _450.cameraInvViewProj * float4(((gl_FragCoord.x / _450.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _450.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_664, fast::normalize((_692 / float4(_692.w)).xyz - _664), 0.001000000047497451305389404296875, _450.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(0u));
    bool _504 = rayQuery.next();
    uint _505 = uint(rayQuery.get_committed_intersection_type());
    int _506 = rayQuery.get_committed_user_instance_id();
    uint _509 = uint(_506);
    niUIGpuFuncs_RayInstanceData _512;
    _512.ibIndex = nil_builtin_GetRayInstanceData[_509]->v.ibIndex;
    _512.vbIndex = nil_builtin_GetRayInstanceData[_509]->v.vbIndex;
    _512.firstIndex = nil_builtin_GetRayInstanceData[_509]->v.firstIndex;
    _512.baseVertexIndex = nil_builtin_GetRayInstanceData[_509]->v.baseVertexIndex;
    _512.texIndex = nil_builtin_GetRayInstanceData[_509]->v.texIndex;
    _512.materialColor = nil_builtin_GetRayInstanceData[_509]->v.materialColor;
    float4 _899;
    if (_505 == 1u)
    {
        float4 _900;
        if (_512.vbIndex == 0u)
        {
            _900 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _524 = rayQuery.get_committed_primitive_id();
            uint _705 = _512.firstIndex + (3u * uint(_524));
            uint _707 = _512.ibIndex;
            uint _710 = _705;
            uint _717 = _705 + 1u;
            uint _724 = _705 + 2u;
            uint _534 = _512.vbIndex;
            uint _540 = _512.baseVertexIndex + nil_builtin_GetIndex[_707]->v[_710];
            niUIGpuFuncs_VertexRay _543;
            _543.pos = float3(nil_builtin_GetVertexRay[_534]->v[_540].pos);
            _543.normal = float3(nil_builtin_GetVertexRay[_534]->v[_540].normal);
            _543.color = nil_builtin_GetVertexRay[_534]->v[_540].color;
            _543.tex0 = float2(nil_builtin_GetVertexRay[_534]->v[_540].tex0);
            uint _552 = _512.baseVertexIndex + nil_builtin_GetIndex[_707]->v[_717];
            niUIGpuFuncs_VertexRay _555;
            _555.pos = float3(nil_builtin_GetVertexRay[_534]->v[_552].pos);
            _555.normal = float3(nil_builtin_GetVertexRay[_534]->v[_552].normal);
            _555.color = nil_builtin_GetVertexRay[_534]->v[_552].color;
            _555.tex0 = float2(nil_builtin_GetVertexRay[_534]->v[_552].tex0);
            uint _564 = _512.baseVertexIndex + nil_builtin_GetIndex[_707]->v[_724];
            niUIGpuFuncs_VertexRay _567;
            _567.pos = float3(nil_builtin_GetVertexRay[_534]->v[_564].pos);
            _567.normal = float3(nil_builtin_GetVertexRay[_534]->v[_564].normal);
            _567.color = nil_builtin_GetVertexRay[_534]->v[_564].color;
            _567.tex0 = float2(nil_builtin_GetVertexRay[_534]->v[_564].tex0);
            float2 _568 = rayQuery.get_committed_triangle_barycentric_coord();
            float _735 = (1.0 - _568.x) - _568.y;
            float2 _896;
            if (_735 > 0.85000002384185791015625)
            {
                _896 = _543.tex0;
            }
            else
            {
                float2 _897;
                if (_568.x > 0.85000002384185791015625)
                {
                    _897 = _555.tex0;
                }
                else
                {
                    _897 = select(((_543.tex0 * _735) + (_555.tex0 * _568.x)) + (_567.tex0 * _568.y), _567.tex0, bool2(_568.y > 0.85000002384185791015625));
                }
                _896 = _897;
            }
            _900 = float4(_896, 0.0, 1.0);
        }
        _899 = _900;
    }
    else
    {
        _899 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_505 == 2u));
    }
    out.OUT_0_rval_color = _899;
    return out;
}

