#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _431;
    _431.rtWidth = IN_1_aUniforms.v.rtWidth;
    _431.rtHeight = IN_1_aUniforms.v.rtHeight;
    _431.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _431.padding = IN_1_aUniforms.v.padding;
    _431.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _431.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _604 = float3(_431.cameraInvView[3].x, _431.cameraInvView[3].y, _431.cameraInvView[3].z);
    float4 _632 = _431.cameraInvViewProj * float4(((gl_FragCoord.x / _431.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _431.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_604, fast::normalize((_632 / float4(_632.w)).xyz - _604), 0.001000000047497451305389404296875, _431.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _484 = rayQuery.next();
    uint _485 = uint(rayQuery.get_committed_intersection_type());
    int _486 = rayQuery.get_committed_user_instance_id();
    uint _489 = uint(_486);
    TestGpuFuncs_RayInstanceData _492;
    _492.ibIndex = nil_builtin_GetRayInstanceData[_489]->v.ibIndex;
    _492.vbIndex = nil_builtin_GetRayInstanceData[_489]->v.vbIndex;
    _492.texIndex = nil_builtin_GetRayInstanceData[_489]->v.texIndex;
    float4 _834;
    if (_485 == 1u)
    {
        float4 _835;
        if (_492.vbIndex == 0u)
        {
            _835 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _504 = rayQuery.get_committed_primitive_id();
            uint _643 = 3u * uint(_504);
            uint _645 = _492.ibIndex;
            uint _648 = _643;
            uint _655 = _643 + 1u;
            uint _662 = _643 + 2u;
            uint _512 = _492.vbIndex;
            TestGpuFuncs_VertexRay _518;
            _518.pos = float3(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_648]].pos);
            _518.normal = float3(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_648]].normal);
            _518.color = nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_648]].color;
            _518.tex0 = float2(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_648]].tex0);
            TestGpuFuncs_VertexRay _527;
            _527.pos = float3(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_655]].pos);
            _527.normal = float3(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_655]].normal);
            _527.color = nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_655]].color;
            _527.tex0 = float2(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_655]].tex0);
            TestGpuFuncs_VertexRay _536;
            _536.pos = float3(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_662]].pos);
            _536.normal = float3(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_662]].normal);
            _536.color = nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_662]].color;
            _536.tex0 = float2(nil_builtin_GetVertexRay[_512]->v[nil_builtin_GetIndex[_645]->v[_662]].tex0);
            float2 _537 = rayQuery.get_committed_triangle_barycentric_coord();
            float4x3 _548 = rayQuery.get_committed_object_to_world_transform();
            _835 = float4(((_548 * float4(fast::normalize(((_518.normal * ((1.0 - _537.x) - _537.y)) + (_527.normal * _537.x)) + (_536.normal * _537.y)), 0.0)) * 0.5) + float3(0.5), 1.0);
        }
        _834 = _835;
    }
    else
    {
        _834 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_485 == 2u));
    }
    out.OUT_0_rval_color = _834;
    return out;
}

