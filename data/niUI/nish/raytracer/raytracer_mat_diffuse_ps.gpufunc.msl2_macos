#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

template<typename T>
struct spvDescriptor
{
    T value;
};

template<typename T>
struct spvDescriptorArray
{
    spvDescriptorArray(const device spvDescriptor<T>* ptr) : ptr(&ptr->value)
    {
    }
    const device T& operator [] (size_t i) const
    {
        return ptr[i];
    }
    const device T* ptr;
};

struct niUIGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct niUIGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct niUIGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint firstIndex;
    uint baseVertexIndex;
    uint texIndex;
    uint materialColor;
};

struct SBO_niUIGpuFuncs_RayInstanceData
{
    niUIGpuFuncs_RayInstanceData_1 v;
};

struct niUIGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct niUIGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    niUIGpuFuncs_VertexRay_1 v[1];
};

struct SBO_GetIndex
{
    uint v[1];
};

struct niUIGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_niUIGpuFuncs_RayUniforms
{
    niUIGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(1)]], constant UBO_niUIGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(2)]], const device spvDescriptor<texture2d<float>>* nil_builtin_GetTexture2D_ [[buffer(0)]], sampler IN_1_aSS [[sampler(0)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(3)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<texture2d<float>> nil_builtin_GetTexture2D {nil_builtin_GetTexture2D_};
    spvDescriptorArray<const device SBO_niUIGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};

    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_niUIGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    niUIGpuFuncs_RayUniforms _855;
    _855.rtWidth = IN_1_aUniforms.v.rtWidth;
    _855.rtHeight = IN_1_aUniforms.v.rtHeight;
    _855.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _855.padding = IN_1_aUniforms.v.padding;
    _855.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _855.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _987 = float3(_855.cameraInvView[3].x, _855.cameraInvView[3].y, _855.cameraInvView[3].z);
    float4 _1105 = _855.cameraInvViewProj * float4(((gl_FragCoord.x / _855.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _855.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_987, fast::normalize((_1105 / float4(_1105.w)).xyz - _987), 0.001000000047497451305389404296875, _855.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(0u));
    bool _1122 = rayQuery.next();
    uint _1169 = uint(rayQuery.get_committed_intersection_type());
    int _1170 = rayQuery.get_committed_user_instance_id();
    uint _1175 = uint(_1170);
    niUIGpuFuncs_RayInstanceData _1179;
    _1179.ibIndex = nil_builtin_GetRayInstanceData[_1175]->v.ibIndex;
    _1179.vbIndex = nil_builtin_GetRayInstanceData[_1175]->v.vbIndex;
    _1179.firstIndex = nil_builtin_GetRayInstanceData[_1175]->v.firstIndex;
    _1179.baseVertexIndex = nil_builtin_GetRayInstanceData[_1175]->v.baseVertexIndex;
    _1179.texIndex = nil_builtin_GetRayInstanceData[_1175]->v.texIndex;
    _1179.materialColor = nil_builtin_GetRayInstanceData[_1175]->v.materialColor;
    float2 _2128;
    int _2134;
    if (_1169 == 1u)
    {
        int _1185 = rayQuery.get_committed_primitive_id();
        float2 _1188 = rayQuery.get_committed_triangle_barycentric_coord();
        float4x3 _1189 = rayQuery.get_committed_object_to_world_transform();
        bool _1242 = _1179.vbIndex == 0u;
        float2 _2126;
        if (_1242)
        {
            _2126 = float2(0.0);
        }
        else
        {
            uint _1332 = _1179.firstIndex + (3u * uint(_1185));
            uint _1334 = _1179.ibIndex;
            uint _1337 = _1332;
            uint _1344 = _1332 + 1u;
            uint _1351 = _1332 + 2u;
            uint _1255 = _1179.vbIndex;
            uint _1260 = _1179.baseVertexIndex + nil_builtin_GetIndex[_1334]->v[_1337];
            niUIGpuFuncs_VertexRay _1263;
            _1263.pos = float3(nil_builtin_GetVertexRay[_1255]->v[_1260].pos);
            _1263.normal = float3(nil_builtin_GetVertexRay[_1255]->v[_1260].normal);
            _1263.color = nil_builtin_GetVertexRay[_1255]->v[_1260].color;
            _1263.tex0 = float2(nil_builtin_GetVertexRay[_1255]->v[_1260].tex0);
            uint _1270 = _1179.baseVertexIndex + nil_builtin_GetIndex[_1334]->v[_1344];
            niUIGpuFuncs_VertexRay _1273;
            _1273.pos = float3(nil_builtin_GetVertexRay[_1255]->v[_1270].pos);
            _1273.normal = float3(nil_builtin_GetVertexRay[_1255]->v[_1270].normal);
            _1273.color = nil_builtin_GetVertexRay[_1255]->v[_1270].color;
            _1273.tex0 = float2(nil_builtin_GetVertexRay[_1255]->v[_1270].tex0);
            uint _1280 = _1179.baseVertexIndex + nil_builtin_GetIndex[_1334]->v[_1351];
            niUIGpuFuncs_VertexRay _1283;
            _1283.pos = float3(nil_builtin_GetVertexRay[_1255]->v[_1280].pos);
            _1283.normal = float3(nil_builtin_GetVertexRay[_1255]->v[_1280].normal);
            _1283.color = nil_builtin_GetVertexRay[_1255]->v[_1280].color;
            _1283.tex0 = float2(nil_builtin_GetVertexRay[_1255]->v[_1280].tex0);
            _2126 = ((_1263.tex0 * ((1.0 - _1188.x) - _1188.y)) + (_1273.tex0 * _1188.x)) + (_1283.tex0 * _1188.y);
        }
        _2134 = _1242 ? (-1) : 1;
        _2128 = _2126;
    }
    else
    {
        _2134 = (_1169 == 2u) ? 2 : 0;
        _2128 = float2(0.0);
    }
    float4 _2137;
    if (_2134 == 1)
    {
        float4 _1459 = unpack_unorm4x8_to_float(_1179.materialColor).zyxw;
        float4 _2136;
        if (_1179.texIndex > 0u)
        {
            uint _1467 = _1179.texIndex;
            _2136 = nil_builtin_GetTexture2D[_1467].sample(IN_1_aSS, _2128) * _1459;
        }
        else
        {
            _2136 = _1459;
        }
        _2137 = _2136;
    }
    else
    {
        float4 _2138;
        if (_2134 == 2)
        {
            _2138 = float4(0.5, 0.0, 0.5, 1.0);
        }
        else
        {
            _2138 = select(float4(0.0), float4(1.0, 0.0, 1.0, 1.0), bool4(_2134 == (-1)));
        }
        _2137 = _2138;
    }
    out.OUT_0_rval_color = _2137;
    return out;
}

