#version 460
#extension GL_EXT_ray_tracing : require

struct lib_shader_RayPayload
{
    vec4 color;
    float hitT;
};

layout(location = 0) rayPayloadEXT lib_shader_RayPayload nil_builtin_RAY_PAYLOAD_0;
layout(set = 7, binding = 0) uniform accelerationStructureEXT IN_1_aAS;
layout(set = 8, binding = 0, rgba8) uniform writeonly image2D IN_1_aOutputImage;

uint uint_None;
vec3 vec3_Zero;

void main()
{
    uint_None = 0u;
    vec3_Zero = vec3(0.0);
    vec2 _222 = vec2(gl_LaunchIDEXT.xy) / vec2(gl_LaunchSizeEXT.xy);
    traceRayEXT(IN_1_aAS, uint_None, 255u, 0u, 0u, 0u, vec3_Zero, 0.001000000047497451305389404296875, normalize(vec3((_222.x * 2.0) - 1.0, ((1.0 - _222.y) * 2.0) - 1.0, -1.0)), 100.0, 0);
    imageStore(IN_1_aOutputImage, ivec2(gl_LaunchIDEXT.xy), nil_builtin_RAY_PAYLOAD_0.color);
}

