#pragma clang diagnostic ignored "-Wmissing-prototypes"
#pragma clang diagnostic ignored "-Wmissing-braces"

#include <metal_stdlib>
#include <simd/simd.h>
#if __METAL_VERSION__ >= 230
#include <metal_raytracing>
using namespace metal::raytracing;
#endif

using namespace metal;

template<typename T, size_t Num>
struct spvUnsafeArray
{
    T elements[Num ? Num : 1];
    
    thread T& operator [] (size_t pos) thread
    {
        return elements[pos];
    }
    constexpr const thread T& operator [] (size_t pos) const thread
    {
        return elements[pos];
    }
    
    device T& operator [] (size_t pos) device
    {
        return elements[pos];
    }
    constexpr const device T& operator [] (size_t pos) const device
    {
        return elements[pos];
    }
    
    constexpr const constant T& operator [] (size_t pos) const constant
    {
        return elements[pos];
    }
    
    threadgroup T& operator [] (size_t pos) threadgroup
    {
        return elements[pos];
    }
    constexpr const threadgroup T& operator [] (size_t pos) const threadgroup
    {
        return elements[pos];
    }
};

intersection_params spvMakeIntersectionParams(uint flags)
{
    intersection_params ip;
    if ((flags & 1) != 0)
        ip.force_opacity(forced_opacity::opaque);
    if ((flags & 2) != 0)
        ip.force_opacity(forced_opacity::non_opaque);
    if ((flags & 4) != 0)
        ip.accept_any_intersection(true);
    if ((flags & 16) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::back);
    if ((flags & 32) != 0)
        ip.set_triangle_cull_mode(triangle_cull_mode::front);
    if ((flags & 64) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::opaque);
    if ((flags & 128) != 0)
        ip.set_opacity_cull_mode(opacity_cull_mode::non_opaque);
    if ((flags & 256) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::triangle);
    if ((flags & 512) != 0)
        ip.set_geometry_cull_mode(geometry_cull_mode::bounding_box);
    return ip;
}

struct TestGpuFuncs_RayUniforms
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct SBO_GetIndex
{
    uint v[1];
};

struct TestGpuFuncs_RayInstanceData
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
    uint firstIndex;
    uint baseVertexIndex;
};

struct TestGpuFuncs_RayInstanceData_1
{
    uint ibIndex;
    uint vbIndex;
    uint texIndex;
    uint firstIndex;
    uint baseVertexIndex;
};

struct SBO_TestGpuFuncs_RayInstanceData
{
    TestGpuFuncs_RayInstanceData_1 v;
};

struct TestGpuFuncs_VertexRay
{
    float3 pos;
    float3 normal;
    uint color;
    float2 tex0;
};

struct TestGpuFuncs_VertexRay_1
{
    packed_float3 pos;
    packed_float3 normal;
    uint color;
    packed_float2 tex0;
};

struct SBO_GetVertexRay
{
    TestGpuFuncs_VertexRay_1 v[1];
};

struct TestGpuFuncs_RayUniforms_1
{
    float rtWidth;
    float rtHeight;
    float cameraFarClipPlane;
    float padding;
    float4x4 cameraInvView;
    float4x4 cameraInvViewProj;
};

struct UBO_TestGpuFuncs_RayUniforms
{
    TestGpuFuncs_RayUniforms_1 v;
};

struct main0_out
{
    float4 OUT_0_rval_color [[color(0)]];
};

fragment main0_out main0(const device void* spvBufferAliasSet9Binding0 [[buffer(0)]], constant UBO_TestGpuFuncs_RayUniforms& IN_1_aUniforms [[buffer(1)]], raytracing::instance_acceleration_structure IN_1_aAS [[buffer(2)]], float4 gl_FragCoord [[position]])
{
    spvDescriptorArray<const device SBO_GetIndex*> nil_builtin_GetIndex {nil_builtin_GetIndex_};
    spvDescriptorArray<const device SBO_TestGpuFuncs_RayInstanceData*> nil_builtin_GetRayInstanceData {nil_builtin_GetRayInstanceData_};
    spvDescriptorArray<const device SBO_GetVertexRay*> nil_builtin_GetVertexRay {nil_builtin_GetVertexRay_};

    const device auto& nil_builtin_GetIndex = *(const device spvUnsafeArray<SBO_GetIndex, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetRayInstanceData = *(const device spvUnsafeArray<SBO_TestGpuFuncs_RayInstanceData, 1>*)spvBufferAliasSet9Binding0;
    const device auto& nil_builtin_GetVertexRay = *(const device spvUnsafeArray<SBO_GetVertexRay, 1>*)spvBufferAliasSet9Binding0;
    main0_out out = {};
    TestGpuFuncs_RayUniforms _446;
    _446.rtWidth = IN_1_aUniforms.v.rtWidth;
    _446.rtHeight = IN_1_aUniforms.v.rtHeight;
    _446.cameraFarClipPlane = IN_1_aUniforms.v.cameraFarClipPlane;
    _446.padding = IN_1_aUniforms.v.padding;
    _446.cameraInvView = IN_1_aUniforms.v.cameraInvView;
    _446.cameraInvViewProj = IN_1_aUniforms.v.cameraInvViewProj;
    float3 _631 = float3(_446.cameraInvView[3].x, _446.cameraInvView[3].y, _446.cameraInvView[3].z);
    float4 _659 = _446.cameraInvViewProj * float4(((gl_FragCoord.x / _446.rtWidth) * 2.0) - 1.0, 1.0 - ((gl_FragCoord.y / _446.rtHeight) * 2.0), 1.0, 1.0);
    raytracing::intersection_query<raytracing::instancing, raytracing::triangle_data> rayQuery;
    rayQuery.reset(ray(_631, fast::normalize((_659 / float4(_659.w)).xyz - _631), 0.001000000047497451305389404296875, _446.cameraFarClipPlane), IN_1_aAS, 255u, spvMakeIntersectionParams(16u));
    bool _500 = rayQuery.next();
    uint _501 = uint(rayQuery.get_committed_intersection_type());
    int _502 = rayQuery.get_committed_user_instance_id();
    uint _505 = uint(_502);
    TestGpuFuncs_RayInstanceData _508;
    _508.ibIndex = nil_builtin_GetRayInstanceData[_505]->v.ibIndex;
    _508.vbIndex = nil_builtin_GetRayInstanceData[_505]->v.vbIndex;
    _508.texIndex = nil_builtin_GetRayInstanceData[_505]->v.texIndex;
    _508.firstIndex = nil_builtin_GetRayInstanceData[_505]->v.firstIndex;
    _508.baseVertexIndex = nil_builtin_GetRayInstanceData[_505]->v.baseVertexIndex;
    float4 _867;
    if (_501 == 1u)
    {
        float4 _868;
        if (_508.vbIndex == 0u)
        {
            _868 = float4(1.0, 0.0, 1.0, 1.0);
        }
        else
        {
            int _520 = rayQuery.get_committed_primitive_id();
            uint _672 = _508.firstIndex + (3u * uint(_520));
            uint _674 = _508.ibIndex;
            uint _677 = _672;
            uint _684 = _672 + 1u;
            uint _691 = _672 + 2u;
            uint _530 = _508.vbIndex;
            uint _536 = _508.baseVertexIndex + nil_builtin_GetIndex[_674]->v[_677];
            TestGpuFuncs_VertexRay _539;
            _539.pos = float3(nil_builtin_GetVertexRay[_530]->v[_536].pos);
            _539.normal = float3(nil_builtin_GetVertexRay[_530]->v[_536].normal);
            _539.color = nil_builtin_GetVertexRay[_530]->v[_536].color;
            _539.tex0 = float2(nil_builtin_GetVertexRay[_530]->v[_536].tex0);
            uint _548 = _508.baseVertexIndex + nil_builtin_GetIndex[_674]->v[_684];
            TestGpuFuncs_VertexRay _551;
            _551.pos = float3(nil_builtin_GetVertexRay[_530]->v[_548].pos);
            _551.normal = float3(nil_builtin_GetVertexRay[_530]->v[_548].normal);
            _551.color = nil_builtin_GetVertexRay[_530]->v[_548].color;
            _551.tex0 = float2(nil_builtin_GetVertexRay[_530]->v[_548].tex0);
            uint _560 = _508.baseVertexIndex + nil_builtin_GetIndex[_674]->v[_691];
            TestGpuFuncs_VertexRay _563;
            _563.pos = float3(nil_builtin_GetVertexRay[_530]->v[_560].pos);
            _563.normal = float3(nil_builtin_GetVertexRay[_530]->v[_560].normal);
            _563.color = nil_builtin_GetVertexRay[_530]->v[_560].color;
            _563.tex0 = float2(nil_builtin_GetVertexRay[_530]->v[_560].tex0);
            float2 _564 = rayQuery.get_committed_triangle_barycentric_coord();
            float4x3 _575 = rayQuery.get_committed_object_to_world_transform();
            _868 = float4(((_575 * float4(fast::normalize(((_539.normal * ((1.0 - _564.x) - _564.y)) + (_551.normal * _564.x)) + (_563.normal * _564.y)), 0.0)) * 0.5) + float3(0.5), 1.0);
        }
        _867 = _868;
    }
    else
    {
        _867 = select(float4(0.0, 0.5, 0.800000011920928955078125, 0.0), float4(1.0, 0.0, 0.0, 1.0), bool4(_501 == 2u));
    }
    out.OUT_0_rval_color = _867;
    return out;
}

