#!/bin/bash
if [[ -z $HAM_PROJECT_DIR ]]; then
    echo "E/HAM_PROJECT_DIR not set !"
    return 1
fi

case $HAM_OS in
    NT*)
        toolset_import_list "repos" "dx9sdk2004" "default" || return 1
        ;;
    *)
        toolset_import_list "repos" "default" || return 1
        ;;
esac
